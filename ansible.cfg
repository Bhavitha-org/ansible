[defaults]

inventory      = inventory.ini
log_path = /var/log/ansible.log  
PRIVATE_KEY_FILE = /home/ec2-user/ansible
REMOTE_USER = ansible

# ask_vault_pass = True
# vault_password_file = ~/.ansible/.mysql_vault

# line6 is creating password file to store and mentioning without entering in terminal


#it should be writeable       #getting err saying, this is not writeable so give permi chmod or run with sudo both are not good practices.....just change file path to home dir


